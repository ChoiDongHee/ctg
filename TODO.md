# 남은 할일 (TODO)

> 마지막 업데이트: 2026-06-03  
> 현재 상태: BLE 연결 완료 / 배터리 파싱 완료 / 명령 채널 확인 필요

---

## 🔴 긴급 (배터리 충전 후 즉시)

### 1. Bonded 연결 + type=03 수신 확인
```bash
python test_paired.py
```
- `client.pair()` 실행 → BONDED 연결
- 9채널 구독 (nRF Connect 방식)
- 15초 내 `BC-73-03-00-...-05-[배터리%]-00` 수신 확인
- **성공 기준**: 브라우저에 배터리% 표시

### 2. 사진 촬영 명령 동작 확인
- type=03 수신 성공 후 진행
- `bytes([0x02, 0x01, 0x01])` 전송 (de5b + ae01 + UART 동시)
- **성공 기준**: 찰칵 소리 + 안경 미디어 개수 증가

### 3. Wi-Fi 핫스팟 활성화
- `bytes([0x02, 0x01, 0x04])` 전송
- Windows Wi-Fi 목록에서 안경 SSID 확인
- 비밀번호 `123456789` 으로 연결
- **성공 기준**: `http://192.168.49.x/files/media.config` 접근 가능

---

## 🟡 주요 기능 (명령 채널 확인 후)

### 4. 파일 자동 싱크
- [ ] Wi-Fi 연결 → HTTP manifest 조회 → 병렬 다운로드
- [ ] 관리자 페이지 파일 브라우저에 사진/영상/오디오 표시
- [ ] 싱크 진행률 WebSocket 실시간 표시

### 5. OPUS 오디오 파이프라인
- [ ] 다운로드된 .opus 파일 자동 WAV 변환
- [ ] Whisper STT 자동 실행 (한국어 large 모델)
- [ ] 관리자 페이지 STT 결과 표시

### 6. 자동화 플로우
- [ ] 연결 → 사진 → Wi-Fi → 싱크 → STT 원클릭 자동화
- [ ] 새 파일 감지 → 자동 다운로드 (주기적 폴링)

---

## 🟢 개선 사항

### 7. BLE 연결 안정성
- [ ] Windows BLE 서비스 검색 타임아웃 원인 분석 (bleak 0.22.3 이슈)
- [ ] 재연결 로직 강화 (현재 3회 재시도)
- [ ] 안경 절전모드 자동 해제 방법 조사 (현재 A1 버튼 필요)

### 8. 관리자 UI 개선
- [ ] 배터리 실시간 게이지 (type=03 수신 후)
- [ ] 사진 썸네일 미리보기
- [ ] 영상 플레이어 내장
- [ ] 파일 삭제/선택 기능

### 9. 추가 기능
- [ ] AI 이미지 생성 (`[0x02, 0x01, 0x06, 0x02, 0x02, 0x02]`)
- [ ] 볼륨 제어 (getVolumeControl / setVolumeControl)
- [ ] 기기 버전 정보 표시

---

## 📋 미확인 사항

| 항목 | 상태 | 비고 |
|------|------|------|
| 명령 전송 채널 | ❓ | de5b/ae01/UART 중 어느 것이 실제 작동? |
| Wi-Fi SSID 형식 | ❓ | 안경 핫스팟 SSID가 무엇? |
| 사진 명령 응답 | ❓ | type=03 수신 시 사진 성공 여부 알 수 있나? |
| 미디어 개수 파싱 | ❓ | type=03에서 imageCount/videoCount 위치? |
| Android HCI 로그 | 선택 | 위 항목 실패 시 최후 수단 |

---

## 🔧 기술 부채

- [ ] `test_*.py` 파일들 → `tests/` 폴더로 이동 후 통합
- [ ] 환경변수로 설정 관리 (`.env` 파일)
- [ ] 배터리 15% 이하 시 명령 차단 로직 추가 (현재 경고만)
- [ ] 로그 레벨 설정 (현재 DEBUG 고정)

---

## ✅ 완료 사항

- [x] FastAPI 관리자 페이지 기본 구조
- [x] BLE 연결 (bleak 0.22.3, Windows)
- [x] M02C_EB50 자동 탐색 (M0* 이름 패턴)
- [x] 9채널 Notify 구독 (nRF Connect 방식)
- [x] bc73 type=03 배터리 파싱 구현
- [x] Wi-Fi 비밀번호 확인 (iOS 소스: `123456789`)
- [x] OPUS→WAV 변환 파이프라인 (Ogg 래핑 포함)
- [x] Whisper STT 연동 (로컬 + 클라우드)
- [x] Android SDK AAR 디컴파일 분석
- [x] nRF Connect 로그 상세 분석
- [x] 자동연결 + keepalive 루프
- [x] WebSocket 실시간 상태 브로드캐스트
